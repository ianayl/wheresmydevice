I didn't want to install .NET as a global user program. Instead I wrote a bootstrap script. To use, run:
```shell
. ./bootstrap.sh     # Source the script so we can obtain the proper environment variables
```
